# Core Framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings
pymongo
motor>=3.3.0

# LLM (AWS Bedrock — Claude via Converse API)
boto3>=1.34
google-genai==1.61.0  # retained for deferred eval/ingestion scripts only; not used by the running service
gunicorn==21.2.0

# Embeddings (REQUIRED runtime deps)
sentence-transformers[onnx]>=3.4.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.41.2,<5.0.0
huggingface-hub>=0.23.5,<1.0.0

# Vector DB client (USED IN CODE)
qdrant-client

# Voice
gtts
pydub

# Panchang / Vedic astrology (powers /api/panchang/today)
# Versions verified locally on Python 3.13: ps.available=True, returns
# tithi/nakshatra/yoga/karana/etc. Skyfield pulls in jplephem + sgp4,
# jyotishganit pulls in pandas + python-dateutil — let pip resolve those.
jyotishganit==0.1.2
skyfield==1.54

# Data Processing
numpy
rank-bm25
scipy>=1.13.0

# Utilities
python-multipart
aiofiles
python-dotenv

# Logging
loguru
email-validator
redis

# Query logging (async SQLite)
aiosqlite

# Concept ontology graph
networkx

# Conversation state machine
transitions

# Multi-model evaluation (optional)
anthropic>=0.39.0
openai>=1.50.0
cohere>=5.0,<6.0
